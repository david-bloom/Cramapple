One-line: a square-cornered CramApple action. Secondary actions are underlined text links, never outlined buttons.

```jsx
<ActionButton variant="primary" disabled={!answer}>Submit answer</ActionButton>
```

One-line: the action band that closes the question pane.

```jsx
<ActionRow note="Question 4 of 12"
  primary={<ActionButton variant="primary" disabled={!answer}>Submit answer</ActionButton>}
  secondary={<ActionButton variant="link">Skip for now</ActionButton>} />
```

One-line: the one-word verdict on a submitted answer.

```jsx
<VerdictChip verdict="incorrect" />
```

Blue for correct, clay for incorrect — never the brand red, which is reserved for chrome and the primary action.

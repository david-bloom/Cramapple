One-line: the breadcrumb bar and the handle that drops the study map.

```jsx
<Breadcrumb items={['Unit 2','2.3 Least-Squares Regression','Question 4']} onOpenMap={open} right="Question 4 of 12" />
```

13px is chrome-only type — never put content a student must read to answer in this bar.

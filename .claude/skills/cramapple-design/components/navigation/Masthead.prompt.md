One-line: the red masthead and type-set wordmark that opens every CramApple plate.

```jsx
<Masthead course="AP Statistics" right={<span>Open Hand</span>} />
```

Bungee is the wordmark and nothing else. No logo file exists — never draw one.

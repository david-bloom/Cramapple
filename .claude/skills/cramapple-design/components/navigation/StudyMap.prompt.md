One-line: the unit-level topic map that drops under the breadcrumb.

```jsx
<StudyMap open={mapOpen} unit="Unit 2 · Exploring Two-Variable Data" onClose={close}
  topics={[{code:'2.3',title:'Least-Squares Regression',done:4,total:8,current:true}]} />
```

It covers the three panes but not the chrome, over `--scrim`. Square corners, no motion — it is simply there or not.

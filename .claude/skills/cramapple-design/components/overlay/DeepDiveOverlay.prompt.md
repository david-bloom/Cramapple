One-line: the full-frame deep dive — the long-form explainer behind a question.

```jsx
<DeepDiveOverlay open={deep} title="Why slope is a mean, not a promise" onClose={close} onCopy={copy}
  sections={[{title:'The claim', body:'…'},{title:'The trap', body:'…'},{title:'The habit', body:'…'}]} />
```

Never fit it inside the centre pane; never let it scroll. Cut copy or use fewer sections.

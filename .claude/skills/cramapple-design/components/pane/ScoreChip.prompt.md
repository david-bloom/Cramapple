One-line: the `3 / 4` score readout that sits in a pane header or a feedback card.

```jsx
<ScoreChip earned={null} total={1} label="Score" />
```

Variants: `size="lg"` for the feedback card. Tone is derived (blue when full, clay when partial, neutral before an attempt) but can be forced.

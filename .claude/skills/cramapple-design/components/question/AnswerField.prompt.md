One-line: the FRQ answer box. Purple cap — everything the student produces is purple.

```jsx
<AnswerField value={text} onChange={setText} onAttach={openCamera} />
```

Never scrolls inside a plate: size `rows` to the space available. The camera glyph is one of only two stroke SVGs in the product.

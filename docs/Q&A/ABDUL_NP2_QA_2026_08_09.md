# Abdul Hanan np2-batch QA — 2026-08-09

## Scope

- Production project: `pcntajvbdfqhbeewmdry`
- Target: all 40 items in the np2 replication batch assigned to Abdul Hanan
  (`scripts/content-seed/replication-batch-2026-08-08/README.md`) — AP Calculus AB
  (10 FRQ + 10 MCQ, `apcalcab-*-np2-*`) and AP Precalculus (10 FRQ + 10 MCQ,
  `apprecalc-*-np2-*`). Excludes the separate `apphy1-*-np2-*` (Physics) batch, which is
  a different reviewer's assignment (still `status='assigned'`, no decisions yet).
- Method: protocol `docs/research/CONTENT_AUTHORING_AND_QA_PROTOCOL.md` §9 (independent
  re-derivation) applied to Abdul's 6 `approve_with_edits` items — the pool his own notes
  already pointed at, per §9.3's finding that targeted, hypothesis-driven checking
  outperforms blind resampling. The other 34 `approve` items were not re-derived (§9.5:
  do not blind-resample already-approved content; §9.3 measured near-zero yield there).

## Abdul's np2 performance vs. his historic track record

| | np2 (n=40, 08-08 07:24–15:50) | All other decisions (n=330, since 07-29) |
| --- | ---: | ---: |
| Approve | 34 (85.0%) | 224 (67.9%) |
| Approve with edits | 6 (15.0%) | 103 (31.2%) |
| Disapprove | 0 (0%) | 3 (0.9%) |
| Decisions with a note | 31 (77.5%) | 141 (42.7%) |
| Edit flags later remediated (new version inserted) | 0 / 6 (0%) | 35 / 103 (34%) |

np2 shows a **lower flag rate** than his usual review pool (15% edits vs. 31%+1%
disapprove = 32% flagged historically) — consistent with np2 being a hand-verified batch
authored under the newer protocol, not a general resample. He also **wrote a note on
77.5% of np2 decisions** vs. 42.7% historically, including on plain `approve` items,
suggesting more careful engagement with this batch than his average pass.

The **remediation gap is not specific to np2** — historically, only 34% of his
`approve_with_edits` flags ever produced a repaired version; most of his edit requests sit
unactioned regardless of batch. np2's 0/6 is worse than that baseline, but n=6 is too small
to read as a distinct signal rather than np2 simply being newer (08-08, one day old) and
not yet reaching whatever remediation queue his other flags eventually clear through.

## Independent re-derivation of the 6 flagged items

1. **`apcalcab-frq-np2-008` (part b) — confirmed genuine numeric defect, now fixed.**
   P(t)=2000e^(0.04t), P(10)=2000e^0.4. Re-derived independently (not read-and-trust):
   e^0.4 = 1.491824698… (9-place precision) ⇒ P(10) = 2983.649396 → **2983.649** to three
   decimals. The stored criterion said 2983.653 — Abdul's flag was correct. Parts (a) and
   (c) were independently re-verified and are unchanged.
2. **`apcalcab-mcq-np2-001` (choice D, "6π") — vague rationale, now sharpened.** Stored
   rationale ("does not correctly apply the chain rule") didn't reproduce the value.
   Re-derived: 6π = π(3)(2), i.e. stacking B's error (dV/dr ∝ r, not r²) and C's error
   (dropping the coefficient 4) into dV/dr=πr.
3. **`apcalcab-mcq-np2-005` (choice D, "42") — vague rationale, now sharpened.**
   Re-derived: 42 = (1)(5)+(3)(9)+(1)(10), using the right endpoint's x-value (3) as the
   width of the [1,3] subinterval instead of its actual width (2). Matches Abdul's own
   traced mechanism.
4. **`apcalcab-mcq-np2-006` (choice C, "e³") — no reproducible mechanism, distractor
   replaced.** Neither Abdul nor this independent pass could find any path from
   dy/dx=2xy, y(0)=3 to e³. Per Abdul's own fallback recommendation ("replace C with a
   distractor with a clear derivable error"), replaced with a verified common error:
   omitting the initial condition (C=0 instead of C=ln 3) ⇒ y=e^(x²) ⇒ y(1)=**e**.
5. **`apcalcab-mcq-np2-007` (choice D, "18") — vague rationale, now sharpened.**
   Re-derived: 18 = (g(0)+g(6))/2 = (0+36)/2, averaging endpoint values as if g were
   linear instead of computing the actual average value. Matches Abdul's traced mechanism.
6. **`apprecalc-mcq-np2-007` — checked, no defect found, not touched.** Abdul's note
   said choices C/D were framed as differing "purely by sign omission," imprecise. The
   stored rationales actually already state the specific mechanism he asked for ("uses
   the sine reference value for π/6 instead of the cosine reference value") — his note
   doesn't match what's currently in the database. No change made.

## Remediation applied

`scripts/content-seed/reviewer-qa-remediation/20260809_apcalcab_np2_abdul_qa_repair.sql`,
run against Production this session. Pattern: per §9.4, new admin-authored version per
item (old published version retired, never edited in place), `owner_remediation_approval`
decision referencing Abdul's original decision, structural QA gate (blank stem/choice
count/duplicate text/correct-key count/blank rationale/competing published version — all
0 blocked), then republished.

Result: 5 items repaired and republished at version 2 —
`apcalcab-frq-np2-008`, `apcalcab-mcq-np2-001`, `apcalcab-mcq-np2-005`,
`apcalcab-mcq-np2-006`, `apcalcab-mcq-np2-007`. `apprecalc-mcq-np2-007` and the remaining
34 `approve` items are unchanged.

# AP Statistics 2026-27 — Session Handoff (2026-07-13)

Prepared by Claude. All work below is **repository/local only** — nothing staged, migrated, or
published (`DECISION-0040`). This session firmed the H0/H1 harness contract and authored the first
pre-G0A draft AP Statistics vertical slice through it.

## Artifacts produced

| Artifact | Owner to act | What it needs |
|----------|--------------|---------------|
| `SUBJECT_HARNESS_COMPILER_INTEGRITY_CHECKS_SPEC_2026_07_13.md` (in `docs/architecture/`) | **Codex** | Implement cross-package integrity checks A–D (inventory→archetype, blueprint↔inventory count, archetype↔capability modality, part↔archetype modality) + tests, in `compiler.ts`. All four current fixtures stay green under them. |
| `ap-statistics-2027-slice-q1…q4.item-package.json` (this dir) | **AP Statistics tutor** (G0A/G4A), then **Codex** (G3V) | Verify statistical correctness of every number/condition/rubric point; then independent slice QA. Draft, not cleared. |
| `AP_STATISTICS_2027_INVENTORY_SPLIT_PROPOSAL.md` (in `docs/product/`) | **Orly** (Curriculum Owner) | Approve/amend the 100 MCQ (26/21/21/16/16) and 70 FRQ split for the content bank. |

## Verification done (local)

- All 4 slice FRQs compile green together through `scripts/subject-harness/compiler.ts` against
  `ap-statistics-2026-27.subject-package.json` + `platform-capabilities.v1.json`:
  `deno run --allow-read docs/content/ap_statistics_2026_27_slice/compile-check.ts` →
  `COMPILE OK, items: 4, plan_sha256 af858299…`. Verified both with hash recompute and as-authored.
- Codex's own harness suite still green (`deno test scripts/subject-harness/` — 9 passed) and
  untouched by this session.
- Each item carries a verified canonical `content_sha256`.

## State / cautions for the next session

- **The harness tree is live.** Codex edited `compiler.ts` + `platform-capabilities.v1.json`
  mid-session (added `deterministic_check_contracts`: `numeric-tolerance` → `{absolute}` only;
  `table-shape` → `{rows,columns}` numbers). Re-read before relying on it; don't stage Codex's
  uncommitted changes.
- **Gates not skipped.** G2V slice authoring nominally follows G0A (tutor approves the fact pack,
  still pending). These four items are a contract proof-case and a concrete draft for that review —
  not a gate bypass, not cleared content, and they manufacture no gate evidence. `QA-pass ≠ launch`.
- **MCQ set not authored.** The full G2V slice also calls for one 3-question MCQ set + one standalone
  MCQ per unit (orchestration §97). Not done this session — next content increment.
- **Inventory modeling flag** (raised in the split proposal): bank counts (100/70) ≠ per-exam-form
  counts (42/4); the harness `inventory.targets` needs a bank-vs-form discriminator before these
  numbers enter a SubjectPackage, or gap-B reconciliation will false-fire. For Codex.

## Suggested next actions (in order)

1. Tutor G0A sign-off on the fact pack + correctness pass on the four draft FRQs.
2. Orly approves the inventory split.
3. Codex implements integrity checks A–D and the inventory bank/form discriminator.
4. Author the MCQ portion of the slice; run the whole slice through G3V.

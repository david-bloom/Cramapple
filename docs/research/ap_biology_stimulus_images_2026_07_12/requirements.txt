# Version embedded in the recovered PNG metadata. This pin alone did not
# reproduce the historical bytes because the old transitive dependencies were
# not recorded. Use requirements-lock.txt for new candidate generation.
matplotlib==3.11.1

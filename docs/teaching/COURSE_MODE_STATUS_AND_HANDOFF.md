# Course Mode — Status & Handoff

STATUS: living handoff | DATE: 2026-08-23 (updated late session: canonical grounding + F4 core merged; F2/F3 learner-state runtime merged (#99); the LIVE WRITE HOOK — F4 real `data_driven` grading + `persistCellState` — built, typechecked, unit-tested green, Fable QA rounds 1–2 remediated, and **now MERGED to `main` via PR #101**. DEV BACKEND PIPELINE NOW LIVE (2026-08-23, decision: launch Dev-first, numeric-entry): `last_attempt_id` migration APPLIED; `ap_statistics 2026-27` exam-pack version CREATED (exam date 2027-05-11); loader RUN → 19 items / 6 cells seeded unreleased; `evaluate-attempt` DEPLOYED with the hook (v15). Remaining in Dev: the live "graded attempt → student_cell_state write" proof. Release to students still gated on D8 + CM-D19. See `COURSE_MODE_RELEASE_PATH_DECISION_BRIEF.md`. **UPDATE 2026-08-24 — FIRST TEMPLATE RELEASED (`lsrl_predict`):** D8 bars APPROVED; CM-D19 stamping BUILT + APPLIED to Dev. David reviewed the 20-instance SME sample across two credibility passes and APPROVED the 3 rebuilt credible items ("these are credible for a high schooler"). The credible reload was applied via MCP (byte-fidelity verified against the local file — prompts, all 12 choices, 3 checks, 3 content_sha256 all match). The first real CM-D19 stamp exposed a latent bug (it jumped `draft→published` in one UPDATE, blocked by the `content_pipeline_guard_publish` on both content_items + content_item_versions; the fail-closed unit test had only exercised the gate-REJECTION path). FIXED via migration `20260824010000` — a two-phase `draft→reviewed_approved→published` stamp satisfying every publish guard (idempotent; D8 gate/ledger/scoping unchanged). `cm_d19_release_template('lsrl_predict', <2026-27 epv>, {sme 20/0, property 200/0, verifier 0}, released_by=David)` returned **ok, 3 instances_stamped**; all 3 are now `status=published` / `review_status=question_review_approved`, and `app.template_releases` recorded release `edde7473` (spot-audit 5/mo, not revoked). Front-end serving switches (publish the 2026-27 epv, add a `home_release_manifest` row, set `profiles.active_exam_pack_version_id`) are STAGED and HELD for David's explicit go — he has flagged he is "not ready for front-end experience." **FULL PROD SYNC done 2026-08-24 (David: "Full Prod sync"):** all 6 course-mode migrations promoted to Prod (`pcntajvbdfqhbeewmdry`), the `grading_results` secret-column security gate closed on Prod+Dev (migration `20260824020000`), and — David chose an isolated version — a new Prod ap_statistics `2026-27` exam_pack_version (`7c5a2975`, separate from the live 296-item `2026` pack) created, the 3 credible lsrl items loaded (byte-verified) and CM-D19-released (3 published, released_by David's Prod id `f5a26c6b`). REMAINING for David on Prod: deploy the `evaluate-attempt` hook via CLI (23-file/287KB, MCP can't) and the serving switches (held). PR #102 merged to main by David.) | AUDIENCE: LLM-first entry point for NEW sessions.
Read this first. It says what exists, what is live where, and what the next workstreams are. Governing detail lives in the two companion docs (below); this doc is the map. The dated **Action log (§8)** at the bottom records what each session changed — skim it after this header.

## 0. Companion docs (read in this order)
1. `docs/teaching/COURSE_MODE_LEARNING_MODEL.md` — decisions (`CM-Dxx`), invariants (`INV-1..6`), the mastery model, current-state facts. The source of truth for WHAT and WHY.
2. `docs/teaching/COURSE_MODE_PILOT_BUILD_PLAN.md` — the Stats pilot plan (F1 / Track A / Track B / F4), acceptance, risks, decisions.
3. `docs/teaching/COURSE_MODE_RELEASE_PATH_DECISION_BRIEF.md` — the release path from "hook merged" to "student sees a cell update": the decisions David owns (D8 bars, the 2026-27 exam-pack version, serving form), the deploy-gate steps that are safe now, and the `app.grading_results` security gate — all grounded in verified Dev state (2026-08-23).
4. This doc — current status + handoff.
Prior/underlying: `LEARNING_SYSTEM.md`, `LEARNING_SYSTEM_STUCK.md`, `TEACHING_AND_PEDAGOGY_DESIGN.md` (cram-era learning loop, grading, data posture).

## 1. One-paragraph orientation
Cramapple is shifting from a 10-day AP cram tool to a year-long "efficient learn" companion for time-scarce, points-driven students (one engine, cram = its compressed final phase). The mastery unit is the **cell = (topic × skill)**, practice is a roll-up. The pilot subject is **AP Statistics**, and the strategy is **supply-engine first** (generate validated practice content) because per-cell item inventory is the binding constraint. The supply engine + its canonical grounding + the F4 servable-content path (schema, generic verifier, loader) are BUILT and merged (§2). The learner-state runtime (F2/F3: `student_cell_state` store + the deterministic evidence-weight/tier classifier) is BUILT and merged (#99). The **live write hook** — the `data_driven` real-grading branch in `evaluate-attempt` + `persistCellState`, which connects a graded attempt to a cell-state write — is now BUILT, typechecked, unit-tested green, and **Fable QA rounds 1–2 remediated (14 round-1 findings + 2 round-2 regressions)**, on open PR #101 pending review/merge + one additive migration apply + a Dev edge-function deploy (§2). The NEXT work is: get that hook reviewed/merged/deployed, release + seed the content (D8 + CM-D19 + the Dev exam-pack), and the student EXPERIENCE/front-end (§3, §7).

## 2. What is DONE and verified (2026-08-23)
Everything below is **merged to `main`** (PRs #93 pilot, #94/#95 grounding, #96 F4-core, #97 loader+QA, #99 F2/F3) **except the live write hook**, which is on an OPEN PR (built + typechecked + unit-tested green, **Fable QA rounds 1–2 done + all findings remediated**; pending review/merge + one additive migration apply + Dev deploy). The generator/catalog code lives in `scripts/course_mode_stats_generator/`.
- **F1 — skill taxonomy + cell registry**: `app.taxonomy_skills` (18) + `app.taxonomy_cells` (131 = topic × skill, from CED fact-pack §3) **applied + verified in Dev**, RLS-enabled, keyed to ap_statistics `taxonomy_source_version dae3c72e-82ca-4960-9552-1b034bd347e5`. Migration: `supabase/migrations/20260823115605_...`.
- **Track A — computational generator**: 5 stdlib-exact procedures (one-prop CI 3.3×3.E, two-prop z-test 3.13×3.E, LSRL predict 5.3×3.B, normal prob 2.11×3.C, summary stats 1.7×3.B). Gated emission, deterministic checks, seeded option-shuffle. **Track B** — one 4.B slot-frame (cell 1.9×4.B, observational-only). Current harness: **400 instances / 5,040 invariant checks + 8 meta-props pass, 0 rejects**; slot-frame 60 / 600.
- **Canonical content grounding (A5 + scenarios)** — the distractor and scenario layers are no longer invented in code, each carries a cited source:
  - `misconceptions.py` — 24-entry catalog; every MCQ distractor cites a source (fact-pack §10 / CED / trusted study guides Albert·Fiveable·Khan) + evidence tier. Generation FAILS on a non-catalog tag.
  - `scenarios.py` — per-procedure FRQ framing (archetype §5 / task verb §6 / modality §7 / validity rules) + context banks; every emitted item carries `scenario_provenance`. Contexts are original synthetic (rights: DECISION-0031/0033, no CB content).
- **F4 CORE — servable-content path** (merged #96, **applied + proven in Dev**): migration `20260823130000_course_mode_f4_servable_content_path.sql` adds `app.content_item_checks` (persist deterministic_checks), `app.content_item_cells` (item→cell tag, composite-FK'd to `app.taxonomy_cells`), and the `data_driven_deterministic` evaluator_strategy value (RLS service_role-only). Generic verifier `supabase/functions/_shared/deterministic-verifier.ts` grades numeric/interval responses against persisted checks (abstains on unknown/unparseable; INV-4); `grading-router.ts` routes the new strategy to a `data_driven` target. **End-to-end proven in Dev** (2026-08-23): a generated instance's checks persisted, cell tag `3.3×3.E` resolved to a registered cell via the composite FK, stayed `review_status NULL`/`draft`, and the verifier graded the Dev-persisted checks correctly (correct/incorrect/abstain); proof rows deleted afterward.
- **F4 intake/loader** (merged #97): `scripts/course_mode_stats_generator/build_load_sql.py` → `out/f4_load_DRAFT.sql`, a fail-closed transactional load that lands each generated item as an UNRELEASED draft plus its `item_package_payload` + `mcq_choices` + `content_item_checks` + `content_item_cells`.
- **F2/F3 — learner-state runtime** (PR #99, **green + Fable-GO + applied to Dev, pending merge**): migration `20260823140000_course_mode_f2_student_cell_state.sql` adds `app.student_cell_state`, the per-learner cell mastery store, keyed by `(user_id, taxonomy_source_version, topic_code, skill_code)` with a composite FK to `app.taxonomy_cells`, `tier`/`due_reason` CHECK constraints, an `updated_at` trigger (`app.set_updated_at()`), and the classifier-input columns (`last_session_id`, `last_template_id`, `last_params_hash`, `last_exposure_at`); RLS service_role-only. `supabase/functions/_shared/cell-state.ts` is the **pure, deterministic tier engine** (INV-4): `classifyWeight` (1.0 fresh-independent / 0.65 same-session / 0.35 assisted-or-unchanged / 0 uncertain-or-assisted), `classifySameSession` (session-id first, else a time window with a clock-skew guard: negative interval → same session), `nextCellState`/`applyAttempt` implementing the tier ladder (unseen→exposed_unverified→supported→independent→confirmed) with INV-5 (supported success provisional) and INV-6 (a miss sets `fragile`, decrements evidence with a floor at 0, never zeroes/demotes). A `content_uncertain` attempt is a distinct event: it records the attempt but changes no tier/evidence. `RULE_ENGINE_VERSION="cell-state-1.0"`. Test suite `cell-state_test.ts` (exhaustive tier×event×weight, incl. a mutation-catching delayed-0.65 confirm-gate assertion) is wired into `minimal-ci.yml`. Now WIRED to live attempts by the write hook below (pending merge).
- **LIVE WRITE HOOK — F4 real grading + persistCellState** (OPEN PR, built + typechecked + unit-tested green; NOT yet merged/deployed). Two connected wirings in one PR:
  - *F4 live-grading branch* (`evaluate-attempt/index.ts`): `data_driven` is split out of the shadow-hold into a real branch that fetches `app.content_item_checks` for the version, grades the response with `gradeAgainstChecks` (`_shared/deterministic-verifier.ts`), and writes `grading_results`/`attempts` through the normal finalize path. **ABSTAIN still HOLDS** for shadow review (no parseable number / unhandled check kind / no persisted checks) — INV-3/INV-4 preserved; nothing falls through to the LLM grader. Graded payload built by the new `buildDeterministicGradedPayload` (`_shared/grading-feedback.ts`). New model id `data-driven-deterministic-verifier`; stamps a `data-driven-deterministic-1.0` deterministic_check.
  - *persistCellState* (`_shared/cell-state-persist.ts`): after every graded/uncertain attempt (the MCQ early-return AND the common finalize), resolve item→cell(s) via `content_item_cells`, resolve `subject_id` by UUID via the attempt's `exam_packs` (never the hyphen/underscore `subject_key`), derive the deterministic event (`graded`+full → correct / +partial → miss / non-graded → `content_uncertain`) and the `AttemptSignal` (assisted from `assistance_state`; `changedSurface` from the item-package `provenance.template_id`+params-hash vs the cell's stored last attempt; `sameSession` via `classifySameSession`), run `applyAttempt`, and write `student_cell_state`. Routed on ATTEMPT/ITEM identity, NOT session presence; a **no-op for untagged (legacy/non-course-mode) items**; best-effort (try/catch — a mastery-write failure never fails the grade). Pure signal derivation is factored into `_shared/cell-state-signals.ts` so it is unit-testable offline; `cell-state-signals_test.ts` + `grading-feedback_test.ts` (+ `grading-partial-credit_test.ts`) wired into `minimal-ci.yml`. Full course-mode Deno suite **101/101 green**; both edge files typecheck under a stubbed Supabase client.
  - **Fable QA round-1 remediated (14 findings, all fixed):** F1 every DB read now error-checks and SKIPS the write (a transient error can't demote a real row from a fabricated `initialCellState` prior); F2 per-attempt idempotency via a new `last_attempt_id` column (a re-grade of the same attempt no longer double-counts evidence) — this is the one additive migration (`20260823150000_...last_attempt.sql`); F3 provenance-less items fall back to the version id as their (template,params) identity so a byte-identical repeat scores 0.35, not 1.0; F4 the full deterministic verdicts persist to `grading_results.shadow_result` and the route stamps `data-driven-deterministic-1.0`; F5 an frq-criterion with no persisted check → hold, not a shrunken-denominator over-grade; F6 an uncertain attempt no longer shifts the changed-surface reference; F7 cross-subject writes are guarded (normalized subject-key compare, skip+log); F8 the cell write is now optimistic-concurrent (read `updated_at` → guarded update/insert → one retry); F10 verdict `reason` strings (which embed the answer value) are redacted from student-facing payloads, full detail only in `shadow_result`; F11 added the missing tests; F12 `submitted_at` is the event time, not grading wall-clock; F14 nits (log a failed checks read, seed-hash robustness, subject_id passthrough).
  - **Design decisions baked in (carry forward):** partial credit on a multi-point item is a full **miss** (conservative — a cell needs full independent success; revisit before multi-point tagged items ship, 601 prod criteria are multi-point); mastery is **at-most-once per (attempt, cell)** — the *first* terminal grade wins, a later re-grade of the same attempt does not reconcile the tier; a crash between the grade commit and the hook drops that one cell write unrecoverably (at-most-once best-effort; acceptable at pilot volume, a durable outbox is the future fix); the F8 optimistic guard covers pilot-volume races but is not a substitute for a server-side atomic transition at scale.
  - **Fable QA round-2 (re-QA) remediated — 2 regressions the round-1 fixes introduced, both fixed:** (1) the F4 audit put the answer-key verdicts in `grading_results.shadow_result`, and the idempotent-replay path returned the whole row → a re-POST leaked it; now both replay paths strip `shadow_result` + `raw_model_response` (`sanitizeGradingResultForClient`). (2) the F2 stamp claimed `last_attempt_id` on `content_uncertain` writes too, so an uncertain-hold-then-successful-regrade of the same attempt was skipped forever; the stamp is now made by the pure `attemptIdempotency` helper that only claims the attempt on an evidence-bearing (graded) write (an uncertain write preserves the prior stamp). Also: a failed `frq_criteria` read now fails the request instead of bypassing the uncovered-criteria hold; `normalizeSubjectKey` maps only separators (no digit-adjacency collisions); the taxonomy-read-failure skip no longer double-logs as a mismatch; the persist idempotency/stamp logic is now unit-tested (`attemptIdempotency` cases). Deno suite **105/105**.
  - **Remaining gates**: review/merge, **apply the `last_attempt_id` migration to Dev BEFORE deploying the edge function** (deploy-before-migration would make every `student_cell_state` read/write error → F1 SKIPs them → the whole hook silently no-ops, grades unaffected; smoke-verify one write lands after deploy), and a Dev edge-function deploy of `evaluate-attempt`. **Still ZERO learner-visible effect until content is released (D8/CM-D19).** RELEASE-GATE VERIFY (re-QA finding 1, config-dependent): confirm students read grading results only via the curated `public.grading_results` view (which excludes `shadow_result`) and that PostgREST does not expose the `app` schema to the `authenticated` role — else a student could read their own row's `shadow_result` (answer key) directly; consider column-level grants before release. NOTE the §4 serving-form decision still governs the response field (numeric-entry vs MCQ): the branch grades `responseText` as numeric-entry; if served as MCQ the choice must map to a numeric response (or route to `mcq_rule`) to score rather than abstain.
- **QA**: **four** Fable QA passes over the extension. F4 round-1 GO-WITH-CONDITIONS → 4 majors + 2 minors fixed (grading fail-open guard, key-passes-own-check, option shuffle, verifier leading-decimal regex, loader dollar-quote guard); F4 round-2 re-QA **GO, no regressions.** F2/F3 round-1 GO-WITH-CONDITIONS → 6 findings fixed (content_uncertain made a distinct no-advance event; confirm-gate given real mutation coverage; clock-skew made conservative; `last_exposure_at` split from `last_attempt_at`; classifier-input columns added; `updated_at` trigger added); F2/F3 round-2 re-QA **GO** (55/55 suite, 960/960 tier sweep, mutant killed).
- **Schema convergence of the 4 pilot tables** (prior): `content_item_versions`, `grading_results`, `exam_pack_versions`, `content_taxonomy_labels` column-identical Dev↔Prod. `content_item_versions.item_package_payload` (jsonb) exists in BOTH envs — the home for generated packages.

## 3. What is NOT built (the next work)
- **The live write hook — now BUILT, Fable-QA-remediated, and on OPEN PR #101 (§2), no longer "not built".** Remaining: review/merge, apply the one additive `last_attempt_id` migration to Dev, a Dev edge-function deploy, and a re-QA. It has ZERO learner-visible effect until content is released (D8/CM-D19), so it is safe to merge/deploy ahead of release. The one open dependency it exposes is the §4 serving-form decision (numeric-entry vs MCQ) — the branch grades `responseText` as numeric-entry.
- **CM-D19 template-release machine-stamping.** NOT built (gated on D8). Until it exists, every generated item stays `review_status NULL` / `unreleased_generated_pending_review` and is never served — which is why F4 core + F2/F3 are safe to have landed without being live.
- **The student EXPERIENCE / front-end.** Not designed or built (per-cell micro-experience by trigger `reason`; the "your 20 minutes" session assembly; the fortress/progress surface). The frontend is a **separate Lovable repo** (`david-bloom/exam-buddy-wireframe`), deployed via Lovable publish — UX work happens there against the backend contract, not in this repo.
- **Seed content at scale (blocked on a Dev prerequisite).** The loader is ready but Dev has **no ap_statistics `2026-27` exam-pack version** (only `2025-26`), so `build_load_sql.py`'s fail-closed `into strict` resolution aborts. Creating that exam-pack version (with an official exam date) is a governance object — a David/Orly decision, not to be fabricated. Once it exists, run the loader; items land unreleased pending CM-D19.
- **t / χ² computational procedures.** scipy dependency is APPROVED (env was stdlib-only). Not yet built; would extend Track A to the inference-heavy computational cells.

## 4. Open decisions (carry forward)
- **D8 — release bars** (validation sample sizes / gold-regression thresholds before a template is "released"): **ON HOLD — David is still thinking.** Do not set defaults or build the release gate / CM-D19 stamping until he decides. This gate is why nothing is served yet.
- **F4 item→cell tag home**: RESOLVED at build → a dedicated `app.content_item_cells` table (NOT a `content_taxonomy_labels` scope `cell`, whose scope/payload constraints are unit/topic-shaped). Composite FK to `taxonomy_cells`.
- **Serving form of generated computational items** (NEW, needed before live-grading wiring): the items are MCQ *and* carry numeric checks. The loader sets `evaluator_strategy=data_driven_deterministic` (numeric-entry grading via the verifier) with `rubric_type NULL`, but `mcq_choices` are also loaded so they *can* serve as MCQ (`rule_based_mcq`). Decide which form the pilot serves before wiring live grading.
- **ap_statistics 2026-27 exam-pack version in Dev** (NEW): must exist before the loader can run (see §3). David/Orly.
- **F2/F3 tunables are design judgment, not settled law** (NEW): the `TUNABLES` in `cell-state.ts` (decay windows 3/10/30d, `provisionalConfirmDays=1`, `newExposureConsolidationDays=2`, `directMissRecheckDays=1`, `sameSessionWindowMinutes=30`, `confirmMinDelayDays=1`, `missEvidencePenalty=0.5`) are Phase-1 defaults chosen to be reasonable and deterministic, not calibrated against learner data. They will want a review/tuning pass once real attempts flow. The `RULE_ENGINE_VERSION` stamp exists so a future tuning change is auditable.
- **Serving-form decision also blocks the cell-state signal** (cross-ref §4 serving form): `classifyWeight` needs a truthful `assisted`/`changedSurface`/`uncertain` signal from the attempt. Whichever serving form is chosen (numeric-entry vs MCQ) must produce those signal fields for the write hook to classify weight correctly.
- **Phase-2 graph triggers** (prereq / thread-sibling) + the CED principle graph: DEFERRED.
- **Extending beyond Stats / science item→topic backfill**: DEFERRED.

Process note (not a decision, but carry it): F2/F3 migrations were applied to Dev via the Supabase MCP (`apply_migration`/`execute_sql`), so Dev's `schema_migrations` timestamps do NOT match the repo migration filenames — same benign pattern as F4. Verify `student_cell_state`'s existence/columns directly (it has 22 columns in Dev), never via `list_migrations`. See §6.

## 5. Settled decisions to respect (do not relitigate)
- Mastery unit = cell (topic × skill); practice = roll-up (CM-D05). Store fine, present coarse (INV-1: never show letter codes to students).
- No general LLM item generation (INV-3): content ships only if its correctness is independently checkable (computation: recompute; conceptual: authored slot-frames/keys).
- Threading resurfaces, never pools evidence (INV-2). Determinism for state transitions (INV-4). Supported success is provisional (INV-5). A miss reopens, never zeroes (INV-6).
- Template-level release (CM-D19) APPROVED with a sampled spot-audit; David is SME reviewer of record (D2) and consciously amended the fact-pack "SME-before-bulk-authoring" precondition.
- Negative-value distractors are acceptable (faithful misconception output). scipy is an acceptable authoring dependency.

## 6. Gotchas for any new session
- **Production has zero real students** — all attempts trace to pilot/David's account (`project_production_zero_real_students`). `content_item_versions` had 8 rows in Dev.
- **Subject-key namespace trap**: registry/subjects use hyphens (`ap-statistics`... actually `subjects.subject_key` hyphenated), taxonomy uses underscores (`ap_statistics`). Join by UUID (`subject_id`, `taxonomy_source_version`), never raw text.
- **Dev migration ledger cannot be trusted** (`project_task0027_schema_convergence_status`): verify object existence/counts directly, not via `list_migrations`.
- Broader Dev↔Prod schema drift beyond these 4 tables still exists (the wider TASK-0027 gap) — out of scope for course-mode; don't assume full parity.

## 7. Suggested first moves for the next sessions
- **Live write hook — MERGED to `main` (PR #101), NOT yet applied/deployed to Dev (verified 2026-08-23).** The LAST MILE is now the deploy-gate, not a review: (a) **apply the `last_attempt_id` migration to Dev FIRST, THEN deploy `evaluate-attempt`** (the deployed fn is still the pre-hook v14; deploy-before-migration silently no-ops the whole hook — see the decision brief §4); (b) smoke-test one write with a throwaway cell-tagged draft; (c) resolve the §4 serving-form decision so the graded response field matches the branch (numeric-entry). Deploy is safe ahead of release (zero learner-visible effect until D8/CM-D19). Full detail + verified Dev state: `COURSE_MODE_RELEASE_PATH_DECISION_BRIEF.md`.
- **Content / release session**: decide D8 with David; then (a) create the ap_statistics 2026-27 exam-pack version in Dev, (b) run `build_load_sql.py` → apply `out/f4_load_DRAFT.sql` to Dev to seed cells (unreleased), (c) build CM-D19 template-release stamping. Optionally add scipy-based t/χ² procedures.
- **UX session**: start from `COURSE_MODE_LEARNING_MODEL.md` §10 + §5 (trigger `reason` → experience mapping); design the per-cell loop and the "20 minutes" session assembly; prototype in the Lovable frontend (`exam-buddy-wireframe`) against a defined backend contract.

## 8. Action log (newest first)
- **2026-08-24 (session 3 — generator "Fix 1": stamp MCQ-served items `rubric_type='mcq'`, code-only):** Applied the session-2 grading-mismatch fix at the source so future generated items grade correctly without a manual DB touch. `scripts/course_mode_stats_generator/build_load_sql.py` now stamps **every** loaded item `rubric_type='mcq'` (was `case when v_is_comp then null else 'mcq' end` — computational items got NULL). Root cause recap: the grading router (`grading-router.ts`) prioritizes `rubric_type` above `evaluator_strategy`; with `rubric_type NULL` + `evaluator_strategy='data_driven_deterministic'`, a CHOICE answer routed to the numeric verifier, which abstained → `content_uncertain`, no cell promotion. `evaluator_strategy` is kept split as an inert data marker (computational → `data_driven_deterministic`, slot-frame → `rule_based_mcq`) so the F4 numeric-verification substrate + persisted `content_item_checks` are preserved for a possible future numeric-entry serving mode; while `rubric_type='mcq'`, the router ignores it and grades on choice-match. This exactly matches the proven-live Dev config (session 2 applied `rubric_type='mcq'` by hand to the 3 released lsrl items). Regenerated `out/f4_load_DRAFT.sql` (clean one-hunk diff, re-serialization byte-stable) and patched the reusable `out/lsrl_reload_DRAFT.sql` so a re-run can't reintroduce the bug. Validation: loader `--check` 28/28 packages 0 problems; router TS unchanged (its committed test already asserts `rubric_type` wins; Deno suite runs in CI — esm.sh proxy-blocked locally). **No DB, no migration, no Dev/Prod touch**; the 3 Dev items already carry the manual fix. Remaining old-NULL location: Prod's 3 items in epv `7c5a2975` (behind the held Prod serving work — `COURSE_MODE_NEXT_SESSION_PROMPT.md` §6.3).
- **2026-08-23 (release-path decision brief — SURFACE, not execute):** Caught up on the whole course-mode extension, then (per David's pick of the RELEASE-PATH workstream) surfaced the path from "hook merged" to "student sees a cell update" without executing any governance/release step. **Discovered PR #101 is now MERGED to `main`** (`571f6a0`) — the handoff header/§2/§7 still called it draft/open, now corrected. **Verified live Dev state** (read-only): the `last_attempt_id` migration is UNAPPLIED (`student_cell_state.last_attempt_id` absent) and `evaluate-attempt` still runs the pre-hook **v14** (~mid-July) — so the deploy-gate ordering hazard (migration-before-deploy) is live, not hypothetical; Dev has only the `ap_statistics 2025-26` exam-pack version (loader still blocked); `taxonomy_cells`=131, `content_item_cells`/`content_item_checks`/`student_cell_state` all 0. **Confirmed the `app.grading_results` exposure as a real surface**: `public.grading_results` correctly excludes `shadow_result`, but `app.grading_results` carries a direct `authenticated: SELECT` + the `shadow_result` column + owner-RLS → own-answer-key leak IF `app` is REST-exposed (verify exposed schemas in the dashboard; recommend revoking the direct grant). Wrote `COURSE_MODE_RELEASE_PATH_DECISION_BRIEF.md` (the decisions David owns — D8 bars, the 2026-27 exam-pack version, serving form; the safe-now deploy-gate G1–G3; the security gate; the CM-D19 build sketch). **Executed nothing**: no migration applied, no deploy, no exam-pack version created, loader not run, CM-D19 not built, no Dev config changed; Prod untouched.
- **2026-08-23 (late session — LIVE WRITE HOOK Fable QA round-2 re-QA + regression remediation, PR #101):** Independent re-QA verified all 14 round-1 findings genuinely fixed and hunted the remediation for regressions (12-scenario fake-client probe of the persist layer). Verdict GO-WITH-CONDITIONS: **2 MAJOR regressions the round-1 fixes introduced, both fixed here.**
  - **Regression 1 (leak) — the F4 audit undone by idempotent replay:** the F4 fix wrote the answer-key verdicts to `grading_results.shadow_result`, and the two replay paths (`select("*")` → return row) handed the whole row back on a same-key re-POST, leaking it (plus `raw_model_response`). Fix: `sanitizeGradingResultForClient` strips both columns on both replay returns. **Release-gate follow-up (config-dependent):** verify students read grading results only via the curated `public.grading_results` view and that PostgREST does not expose `app.*` to `authenticated`, or add column-level grants — else a direct REST read of one's own row still exposes `shadow_result`.
  - **Regression 2 (lost write) — the F2 stamp consumed the budget on zero-evidence writes:** `last_attempt_id` was stamped on `content_uncertain` writes too, so an uncertain hold (or an LLM-timeout "failed") followed by a successful re-grade of the same attempt was skipped forever. Fix: extracted a pure `attemptIdempotency(priorAttemptId, attemptId, event)` (in `cell-state-signals.ts`, unit-tested) — SKIP a re-grade of the same attempt; STAMP only on an evidence-bearing (graded) write; a `content_uncertain` write preserves the prior stamp.
  - MINOR/NIT also fixed: a failed `frq_criteria` read now fails the request (was: silently bypassed the F5 uncovered-criteria hold); `normalizeSubjectKey` maps only separators (no `ap-stats-2`/`apstats2` digit-adjacency collision); the taxonomy-read-failure skip no longer double-logs as a phantom cross-subject mismatch; the persist layer's idempotency/stamp decision now has automated coverage (previously untested — the gap that hid Regression 2). Accepted-as-documented: the bounded 2-try optimistic loop can drop one write under 3-way contention (pilot-adequate; server-side atomic transition is the scale fix); at-most-once best-effort on a crash between grade-commit and hook.
  - **Validation**: full course-mode Deno suite **105/105 green**; both edge files typecheck. Prod/Dev untouched (code + the one unapplied migration).
- **2026-08-23 (late session — LIVE WRITE HOOK Fable QA round-1 + full remediation, PR #101):** Independent Fable-model adversarial QA of the write-hook diff returned GO-WITH-CONDITIONS with 14 findings (2 BLOCKER, 3 MAJOR, rest MINOR/NIT); **all 14 remediated in the same PR.**
  - BLOCKERs: F1 — every `persistCellState` DB read now error-checks and SKIPS the write (a transient read error could previously demote/reset a real cell row via a fabricated `initialCellState` prior); F2 — per-attempt idempotency via a new nullable `last_attempt_id` column (`20260823150000_..._cell_state_last_attempt.sql`) so a re-grade of the same attempt under a new idempotency key no longer double-counts evidence (at-most-once per (cell, attempt), first grade wins).
  - MAJORs: F3 — provenance-less (authored) items fall back to the version id as their (template,params) identity, so a byte-identical repeat scores 0.35 not 1.0 (no false promotion to `confirmed`); F4 — the full deterministic verdicts now persist to `grading_results.shadow_result` and the route stamps `deterministic_verifier_version = data-driven-deterministic-1.0` (graded path was previously unauditable); F5 — an `frq_criteria` criterion with no persisted check now HOLDS the item for review instead of grading a shrunken denominator (over-grade).
  - MINOR/NIT: F6 — an uncertain attempt no longer shifts the changed-surface reference; F7 — cross-subject writes guarded (normalized subject-key compare, skip+log); F8 — the cell write is optimistic-concurrent (read `updated_at` → guarded update/insert → one retry); F10 — verdict `reason` strings (which embed the answer value) redacted from student-facing payloads, full detail only in `shadow_result`; F11 — added tests (`buildDeterministicGradedPayload` in `grading-feedback_test.ts`; same-surface-never-confirmed, partial-provenance, non-finite-score in `cell-state-signals_test.ts`) + wired `grading-feedback_test.ts` and `grading-partial-credit_test.ts` into `minimal-ci.yml`; F12 — `submitted_at` is the event time, not grading wall-clock; F14 — nits (log a failed checks read, robust seed hash, subject_id passthrough).
  - Design decisions recorded (§2): partial credit = full miss (conservative; revisit before multi-point tagged items); mastery at-most-once per (attempt, cell); F8 optimistic guard is pilot-volume-adequate (a server-side atomic transition is the scale fix); a crash between grade-commit and hook drops one cell write (at-most-once best-effort).
  - **Validation**: full course-mode Deno suite **101/101 green** (was 62; +grading-feedback/partial-credit into CI + new cases); both edge files typecheck under the stubbed client. Prod/Dev untouched — code + one unapplied migration. **The migration MUST be applied to Dev before the edge function is deployed.**
- **2026-08-23 (late session — LIVE WRITE HOOK: F4 real grading + persistCellState, OPEN PR, built + typechecked + unit-tested green, pending review/merge + Dev deploy + Fable QA):**
  - `supabase/functions/evaluate-attempt/index.ts` — split `data_driven` out of the shadow-hold into a real grading branch: fetch `app.content_item_checks` for the version → `gradeAgainstChecks` (`_shared/deterministic-verifier.ts`) → graded via the common finalize path. ABSTAIN (no parseable number / unhandled check kind / no persisted checks) still HOLDS for shadow review (INV-3/INV-4); nothing falls through to the LLM grader. New model id `data-driven-deterministic-verifier`; `data-driven-deterministic-1.0` deterministic_check stamped.
  - `supabase/functions/_shared/grading-feedback.ts` — new `buildDeterministicGradedPayload` producing the graded finalPayload (per-criterion earned/not, real points, highest_value_gap) from the verifier's pass/fail verdicts, matching the shape the finalize path already consumes.
  - `supabase/functions/_shared/cell-state-persist.ts` — new `persistCellState`, called after both graded paths (MCQ early-return + common finalize). Resolves item→cell(s) via `content_item_cells`; `subject_id` by UUID via the attempt's `exam_packs` (never the hyphen/underscore `subject_key`); derives the deterministic event + `AttemptSignal` (assisted from `assistance_state`; `changedSurface` from item-package `provenance.template_id`+params-hash vs the cell's stored last attempt; `sameSession` via `classifySameSession`); runs `applyAttempt`; UPSERTs `student_cell_state`. Routed on ATTEMPT/ITEM identity, not session presence; **no-op for untagged items**; best-effort (never fails the grade).
  - `supabase/functions/_shared/cell-state-signals.ts` (+ `cell-state-signals_test.ts`, 18 tests incl. a signals→engine ladder walk) — pure signal derivation split out so it is unit-testable offline (the Supabase client can't be resolved offline); test wired into `minimal-ci.yml`.
  - **Validation**: full course-mode Deno suite **62/62 green**; both Supabase-importing edge files typecheck under a stubbed client (esm.sh is proxy-blocked locally, same constraint CI works under). No migration (F2/F4 tables already in Dev). Prod/Dev untouched by this session — code-only PR.
- **2026-08-23 (late session — F2/F3 learner-state runtime, PR #99, merged; green + Fable-GO + applied to Dev):**
  - `20260823140000_course_mode_f2_student_cell_state.sql` — `app.student_cell_state` cell mastery store: composite FK to `taxonomy_cells`, tier/due_reason CHECKs, `updated_at` trigger, classifier-input columns, RLS service_role-only.
  - `supabase/functions/_shared/cell-state.ts` — pure deterministic tier engine (`classifyWeight`, `classifySameSession`, `nextCellState`, `applyAttempt`, `content_uncertain` distinct event, `RULE_ENGINE_VERSION="cell-state-1.0"`); `cell-state_test.ts` exhaustive suite wired into `minimal-ci.yml`. NOT yet called from the request path (that is the live write hook, §3/§7).
  - Two Fable QA rounds on F2/F3: round-1 GO-WITH-CONDITIONS → 6 findings fixed (content_uncertain distinct no-advance event; real mutation coverage on the confirm gate; conservative clock-skew; `last_exposure_at` split from `last_attempt_at`; classifier-input columns; `updated_at` trigger) → round-2 re-QA **GO** (55/55 suite, 960/960 tier sweep, mutant killed).
  - **Dev apply**: migration applied + verified in Dev (`wmgjsdkphcyhngaffbqf`, `student_cell_state` 22 columns, empty). Prod (`pcntajvbdfqhbeewmdry`) untouched. Dev migration ledger diverges from repo filenames (MCP-apply) — verify objects directly (§4 process note, §6).
- **2026-08-23 (late session — grounding + F4 core, all merged to `main`):**
  - `misconceptions.py` canonical distractor catalog (A5) — PR #94 (merged).
  - `scenarios.py` canonical FRQ-framing + `scenario_provenance` on every item — PR #95 (merged; note PR #94's merge raced past this commit, so it was recovered as its own PR).
  - F4 core — `content_item_checks` + `content_item_cells` migration, `deterministic-verifier.ts`, router `data_driven` target, tests — PR #96 (merged).
  - F4 intake/loader (`build_load_sql.py`) + Fable QA remediation (grading fail-open guard, key-passes-own-check + summary_stats tol, option shuffle, verifier leading-decimal regex, loader guards) — PR #97 (merged).
  - Two Fable QA passes: GO-WITH-CONDITIONS → all conditions fixed → re-QA GO, no regressions.
  - **Dev apply**: F4 migration applied + verified in Dev (`wmgjsdkphcyhngaffbqf`); end-to-end servable path proven with a throwaway proof row (checks persist, cell FK resolves, verifier grades correctly), then deleted. Prod (`pcntajvbdfqhbeewmdry`) untouched.
- **2026-08-23 (earlier — pilot):** F1 registry, Track A generator + Track B slot-frame, 4-table schema convergence — PR #93 (merged). (Original Fable QA GO on the generator.)

# Copy to `dev.secrets.env` (same directory) and fill in the DEVELOPMENT
# connection string. `dev.secrets.env` is gitignored (*.secrets.env) — never
# commit it, never paste the value into chat.
#
# Get it from: Supabase Dashboard -> project "Cramapple - Development"
#   (wmgjsdkphcyhngaffbqf) -> Project Settings -> Database -> Connection string
#   -> "Session pooler" or "Direct connection" (URI form).
#
# It must be the DEVELOPMENT project. The applier also refuses to run against any
# database lacking the Dev-only task0016_phase_a_* migration markers.

export CRAMAPPLE_DEV_DB_URL='postgresql://postgres:PASSWORD@db.wmgjsdkphcyhngaffbqf.supabase.co:5432/postgres'
